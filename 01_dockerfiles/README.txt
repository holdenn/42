Important :

Un container doit etre ephemere, cad que dans l'idee, on doit pouvoir le supprimer et en redemarrer un nouveau avec aucune config supplementaire (grace justement a un dockerfile pousse, etc.)

On a des infos sur la taille du build lorsqu on build, voir la ligne "Sending build context..", avec la commande -f on peut specifier un autre dosssier que le pwd
